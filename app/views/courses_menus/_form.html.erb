<%= form_for(@courses_menu) do |f| %>
  <% if @courses_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@courses_menu.errors.count, "error") %> prohibited this courses_menu from being saved:</h2>

      <ul>
      <% @courses_menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :course_name %><br>
    <%= f.text_field :course_name %>
  </div>
  <div class="field">
    <%= f.label :day %><br>
    <%= f.text_field :day %>
  </div>
  <div class="field">
    <%= f.label :menu_id %><br>
    <%= f.text_field :menu_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
